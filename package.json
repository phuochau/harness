{
  "name": "pi-multi-agent-harness",
  "version": "0.1.0",
  "description": "Durable multi-agent orchestration for Pi",
  "type": "module",
  "engines": {
    "node": ">=22.22.2"
  },
  "bin": {
    "harness": "./bin/harness.mjs"
  },
  "exports": {
    ".": "./dist/index.js",
    "./extension": "./dist/pi/extension.js"
  },
  "files": [
    "dist",
    "bin",
    "presets",
    "src/defaults"
  ],
  "keywords": [
    "pi-package"
  ],
  "pi": {
    "extensions": [
      "./dist/pi/extension.js"
    ]
  },
  "scripts": {
    "prepack": "npm run build",
    "build": "node scripts/clean-dist.mjs && tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build && vitest run",
    "check:phase1": "npm run typecheck && npm run build && vitest run test/unit/contracts test/unit/config test/unit/graph test/integration/package-smoke.test.ts",
    "check:phase2": "npm run check:phase1 && vitest run test/unit/state test/unit/core test/integration/controller-race.test.ts test/e2e/fake-controller.test.ts",
    "check:phase3": "npm run check:phase2 && vitest run test/integration/git test/integration/pi-process test/integration/pi-worker test/contract",
    "check:phase4": "npm run check:phase3 && vitest run test/integration/speckit test/integration/pi-loader.test.ts test/integration/pi-residency.test.ts test/integration/init.test.ts test/unit/pi-commands.test.ts",
    "verify": "npm run typecheck && npm run build && vitest run && npm pack --dry-run --json",
    "compat:speckit": "HARNESS_COMPAT_SPECKIT=1 vitest run test/integration/speckit/preset.test.ts",
    "compat:pi": "HARNESS_COMPAT_PI=1 vitest run test/integration/pi-loader.test.ts",
    "e2e:real": "npm run build && HARNESS_E2E_REAL=1 vitest run test/e2e/pi-native-real.test.ts"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "0.86.1",
    "typebox": "1.3.34"
  },
  "dependencies": {
    "ajv": "8.20.0",
    "ajv-formats": "3.0.1",
    "commander": "15.0.0",
    "execa": "10.0.1",
    "proper-lockfile": "4.1.2",
    "yaml": "2.9.1"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "0.86.1",
    "@types/node": "26.6.2",
    "@types/proper-lockfile": "4.1.4",
    "fast-check": "4.10.2",
    "typebox": "1.3.34",
    "typescript": "7.0.2",
    "vitest": "5.0.1"
  }
}
