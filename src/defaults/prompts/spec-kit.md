Use the installed Spec Kit workflow as the planning source of truth.

Follow the requested `/speckit.*` stage exactly. Write only the declared artifacts for that
stage, preserve prior approved artifacts, and include stable requirement and acceptance IDs.
For the tasks stage, emit dependency metadata that the harness can deterministically validate.
